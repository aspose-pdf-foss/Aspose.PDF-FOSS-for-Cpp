#pragma once

// =============================================================================
// Aspose::Pdf::Facades::ImageMergeMode — layout direction when
// PdfFileEditor::Concatenate stitches images into a single PDF.
// Mirrors canonical Aspose.Pdf.Facades.ImageMergeMode. Note the
// canonical values START AT 1 (not 0).
// =============================================================================

namespace Aspose::Pdf::Facades {

enum class ImageMergeMode : int {
    Vertical = 1,
    Horizontal = 2,
    Center = 3,
};

}  // namespace Aspose::Pdf::Facades
