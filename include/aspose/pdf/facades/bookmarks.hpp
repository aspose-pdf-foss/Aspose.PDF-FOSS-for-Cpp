#pragma once

// =============================================================================
// Aspose::Pdf::Facades::Bookmarks — see bookmark.hpp. Bookmark and its
// collection are mutually recursive, so both live in bookmark.hpp; this
// header re-exports Bookmarks for the per-class include convention.
// =============================================================================

#include <aspose/pdf/facades/bookmark.hpp>
