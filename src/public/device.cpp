// Aspose::Pdf::Devices::Device is the abstract root of the device
// hierarchy with no public members in the canonical assembly. The
// virtual destructor and protected default ctor are defined inline
// in the header. This translation unit exists so the linker has
// a valid body file path to compose against; it emits no symbols.
#include <aspose/pdf/device.hpp>
