// Aspose::Pdf::BitmapInfo is a header-only POD — both the ctor
// and all four accessors are inline in the header. This
// translation unit exists so the linker has a valid body file
// path to compose against; it emits no symbols.
#include <aspose/pdf/bitmap_info.hpp>
