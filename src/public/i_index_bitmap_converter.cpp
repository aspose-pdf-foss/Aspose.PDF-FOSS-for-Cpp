// Aspose::Pdf::IIndexBitmapConverter is a pure-virtual interface
// — every member is declared inline in the header (the destructor
// has `= default` and the three Get*BppImage methods are abstract).
// This translation unit exists so the linker can resolve the
// capability without a missing-body link error; it intentionally
// emits no symbols.
#include <aspose/pdf/i_index_bitmap_converter.hpp>
